from setuptools import setup, find_packages

setup(
    name='task_cli',
    version='0.0.1',
    description='A lightweight task_cli used to create data',
    author='Belam Muia',
    packages=find_packages()
)
